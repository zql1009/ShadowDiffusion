# 快速安装指南

## 问题说明

原始的 `requirements.txt` 存在以下**阻止安装**的问题:

1. **5个包使用本地conda路径** - 在其他机器上不存在:
   ```
   certifi @ file:///opt/conda/conda-bld/certifi_1655968806487/work/certifi
   mkl-random @ file:///tmp/build/80754af9/mkl_random_1626179032232/work
   numpy @ file:///opt/conda/conda-bld/numpy_and_numpy_base_1653915516269/work
   typing_extensions @ file:///tmp/abs_ben9emwtky/croots/recipe/typing_extensions_1659638822008/work
   six @ file:///tmp/build/80754af9/six_1644875935023/work
   ```

2. **1个包版本不存在于PyPI**:
   - `mkl-fft==1.3.1` (PyPI最低版本是1.3.8)

## 快速解决方案

使用修复后的依赖文件:

```bash
# 创建虚拟环境(推荐)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或 venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.fixed.txt

# 验证安装
python -c "import torch; import cv2; import wandb; print('安装成功!')"
```

## 如果遇到torch安装问题

```bash
# 先单独安装PyTorch (根据你的CUDA版本)
# CUDA 11.7
pip install torch==1.13.1+cu117 torchvision==0.14.1+cu117 -f https://download.pytorch.org/whl/torch_stable.html

# 然后安装其他依赖
pip install -r requirements.fixed.txt
```

## 修复内容

`requirements.fixed.txt` 做了以下最小化修改:

- ✅ 移除所有本地文件路径,使用标准PyPI版本
- ✅ 修复 `mkl-fft` 版本号
- ✅ 移除 `mkl-random` (numpy自带随机函数)
- ✅ 保持所有其他依赖不变,确保项目能运行

## 测试项目

安装完成后测试:

```bash
# 测试主要脚本
python sr.py --help
python infer.py --help
python sample.py --help
```

## 故障排除

### 问题: numpy版本冲突
```bash
pip install numpy==1.21.6
```

### 问题: torchaudio版本找不到
```bash
# torchaudio==0.7.0a0 可能找不到,可以跳过或安装兼容版本
pip install torchaudio==0.13.1
```

### 问题: CUDA不匹配
检查你的CUDA版本:
```bash
nvidia-smi
```
然后从 https://pytorch.org/ 安装对应的torch版本。
