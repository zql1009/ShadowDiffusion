# 🚀 Chatbot Web化 - 5分钟快速开始

## 📁 您现在拥有的文件

```
chatbot_web.py       # Gradio版本（推荐）
chatbot_flask.py     # Flask版本（可自定义界面）
start_chatbot.sh     # 一键启动脚本
chatbot使用说明.md    # 详细文档
快速开始.md          # 本文件
```

## ⚡ 最快启动方式

### 方法1: 一键启动（最简单）

```bash
./start_chatbot.sh
```

选择1（Gradio）或2（Flask），然后在浏览器打开对应地址。

### 方法2: 直接运行

**Gradio版本（推荐）：**
```bash
python3 chatbot_web.py
# 打开 http://localhost:7860
```

**Flask版本：**
```bash
python3 chatbot_flask.py
# 打开 http://localhost:5000
```

## ⚠️ 必须先做这个！

### 1. 安装依赖

```bash
# Gradio版本需要
pip install gradio torch

# Flask版本需要
pip install flask torch
```

### 2. 修改模型路径

编辑 `chatbot_web.py` 或 `chatbot_flask.py`，找到这行：

```python
checkpoint_path = "data/save/cb_model/cornell movie-dialogs corpus/2-2_500/4000_checkpoint.tar"
```

改成您的实际模型路径！

## 🎯 如何获取模型文件？

### 如果您在Jupyter Notebook中训练的模型：

在notebook最后添加并执行：

```python
# 保存模型
import torch
torch.save({
    'iteration': iteration,
    'en': encoder.state_dict(),
    'de': decoder.state_dict(),
    'en_opt': encoder_optimizer.state_dict(),
    'de_opt': decoder_optimizer.state_dict(),
    'loss': loss,
    'voc_dict': voc.__dict__,
    'embedding': embedding.state_dict()
}, 'chatbot_model.tar')

print("模型已保存到: chatbot_model.tar")
```

然后在 `chatbot_web.py` 中修改：
```python
checkpoint_path = "chatbot_model.tar"
```

### 如果还没训练模型：

您需要先运行您的notebook代码训练模型。训练完成后会自动保存checkpoint。

## 📱 访问方式对比

| 方式 | Gradio | Flask |
|------|--------|-------|
| 端口 | 7860 | 5000 |
| 界面 | 自动生成，现代化 | 自定义HTML |
| 难度 | 简单 | 中等 |
| 自定义 | 有限 | 完全自由 |
| 推荐 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

## 🌐 让其他人访问

### 局域网访问（同一WiFi）

1. 查看Ubuntu的IP：
```bash
hostname -I
# 例如: 192.168.1.100
```

2. 其他设备浏览器访问：
```
http://192.168.1.100:7860  (Gradio)
http://192.168.1.100:5000  (Flask)
```

### 公网访问（临时）

修改 `chatbot_web.py` 中：
```python
demo.launch(
    server_name="0.0.0.0",
    server_port=7860,
    share=True,  # 改为True!
)
```

重启后会显示一个 `https://xxxxx.gradio.live` 链接，72小时有效。

## 🎨 界面预览

### Gradio界面
```
┌─────────────────────────────┐
│  🤖 PyTorch Chatbot         │
│  基于Cornell Movie Dialogs  │
├─────────────────────────────┤
│  💬 聊天框                   │
│  User: hello                │
│  Bot:  hello .              │
├─────────────────────────────┤
│  [输入框] [提交]             │
└─────────────────────────────┘
```

### Flask界面
```
漂亮的渐变紫色背景
┌─────────────────────────────┐
│  🤖 PyTorch Chatbot         │
│  开始对话吧！                │
├─────────────────────────────┤
│  Bot: 你好！我是AI助手...    │
│                             │
│  User: hello           [你] │
│  [Bot] hello .              │
├─────────────────────────────┤
│  [输入框___________] [发送]  │
└─────────────────────────────┘
```

## 🐛 常见问题速查

### 问题1: 找不到模型
```
错误: 找不到模型文件 xxx.tar
```
**解决**: 检查路径，使用绝对路径如 `/home/user/chatbot_model.tar`

### 问题2: 端口被占用
```
Address already in use
```
**解决**:
```bash
# Gradio改端口
demo.launch(server_port=8080)

# Flask改端口
app.run(port=8080)
```

### 问题3: 没有gradio
```
ModuleNotFoundError: No module named 'gradio'
```
**解决**:
```bash
pip install gradio
```

### 问题4: CUDA错误
**解决**: 在文件开头强制CPU模式
```python
USE_CUDA = False
device = torch.device("cpu")
```

## 💡 测试聊天

启动后试试这些：
- "hello"
- "how are you?"
- "what is your name?"
- "where are you from?"
- "goodbye"

## 📊 两个版本怎么选？

**选Gradio如果：**
- ✅ 想快速搭建，不想写前端
- ✅ 需要专业的聊天界面
- ✅ 想要自动的历史记录管理

**选Flask如果：**
- ✅ 想完全自定义界面样式
- ✅ 需要集成到现有系统
- ✅ 需要更多的API控制

## 🎯 下一步

1. **优化模型**: 用更多数据训练
2. **美化界面**: 修改CSS样式（Flask版本）
3. **添加功能**:
   - 保存聊天记录到数据库
   - 支持语音输入
   - 添加表情支持
4. **部署上线**: 使用nginx + gunicorn

## 📞 需要帮助？

看详细文档: `chatbot使用说明.md`

---

**制作:** Claude Code
**时间:** 5分钟内搞定！
